brew "mise"
brew "opentofu"
brew "tflint"
# A local Kubernetes cluster to apply `deploy/` to. Not needed to build or test the product —
# needed to find the four things that are only wrong once a real API server sees them.
brew "kind"
brew "kubernetes-cli"
brew "rustup"
brew "uv"
