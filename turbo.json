{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**", "build/**", "src/routeTree.gen.ts"],
      "inputs": ["src/**", "index.html", "vite.config.ts", "tsconfig.json"],
      "passThroughEnv": ["NEXT_PHASE", "DATABASE_URL"]
    },
    "check-types": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "tsconfig.json", "package.json"],
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^build"],
      "persistent": true,
      "cache": false
    }
  }
}
