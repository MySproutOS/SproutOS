[package]
name = "sproutos-tenant-auth"
description = "Tenant identification from connection credentials"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
argon2 = { workspace = true }
hex = { workspace = true }
rand = { workspace = true }
sha2 = { workspace = true }
sproutos-srn = { workspace = true }
subtle = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
# Only for the shared naming vectors — `fixtures/naming-vectors.json` is the contract this crate
# and `lib/typescript/services` both have to satisfy, and reading it beats copying it.
serde_json = { workspace = true }
